# Changelog

`YYYY-MM-DD HH:MM BST`, AI Name (Tool), Platform (Desktop / Mobile / CI), Short description of change and why, including primary file(s) in backticks.

All notable changes to this repository are documented here — **newest first**. Use **BST** for the changelog so it stays aligned with `index.html` topbar timings.

| Date · time (BST) | AI Name | Platform | Changed |
|---|---|---|---|
| 2026-05-26 14:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | **World Cup 2026 integration shipped (v2.51)** — full doc pass. API-Football's free tier blocked 2026, so the script now pulls from public-domain `openfootball/worldcup.json` (no key). All 104 fixtures live (group stage opens Thu 11 Jun 20:00 BST Mexico v South Africa; final Sun 19 Jul 20:00 BST). Same script doubles as result-marker. Backlog updated with 3 open items: switch cron to daily during the tournament, verify W99/L101 knockout placeholders resolve mid-tournament, and improve agent research for international fixtures (Understat is club-only). |
| 2026-05-26 12:00 BST | Claude (Claude Code, Opus 4.7) | Desktop | World Cup 2026 integration — phase 1 (fetch). New `worldcup` league entry + "International" sidebar group in `index.html`; new `scripts/fetch-worldcup.js` calling api-football.com v3 (`/fixtures?league=1&season=2026`); new `.github/workflows/fetch-worldcup.yml` (Monday 09:00 UTC, after the regular fetch-fixtures cron). Requires `API_FOOTBALL_KEY` repo secret. Mark-results workflow for WC scores still to come (phase 2). |
| 2026-05-25 13:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | **Incident + fix:** Monday auto-fetch (`a675e56`) wiped fixture analysis because `scripts/fetch-fixtures.js` `formatFixtureLiteral()` predated the v2.33 schema rework — it dropped `homeWin`/`draw`/`awayWin`/`homeForm`/`awayForm` and renamed `momentum` → `drawRates` on every re-serialization. Reverted the bad commit (`git revert a675e56` → `a69a22e`) and patched `formatFixtureLiteral` to detect the schema in use and round-trip whichever fields are present. Stub fixtures created by the script now use the new schema with a neutral 33/34/33 split. Sister bug to the v2.21 fix (`teamNews`/`context`/`bookOdds` preservation). |
| 2026-05-25 12:00 BST | github-actions[bot] | CI | Auto-marked 17 weekend results (Sunderland 2-1 Chelsea, Brighton 0-3 Man United, Crystal Palace 1-2 Arsenal, +14 more); bumped to v2.48. |
| 2026-05-23 01:00 BST | Claude (Claude Code, Opus 4.7) | Desktop | v2.47: per-fixture HIT / MISS prediction badge plus symmetric outcome tags (`Home Win` blue, `Draw` gold, `Away Win` red) on past-fixture score lines. Badge rule: highest-probability outcome matches actual → ✅ HIT (green); mismatch → ❌ MISS (red); Low-confidence picks get no badge. New helper `predictionAccuracy(f)`, CSS classes `.home-tag`, `.away-tag`, `.acc-badge`, `.acc-hit`, `.acc-miss` (`index.html`). |
| 2026-05-23 00:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | v2.46: per-fixture Google search link shipped from backlog. (Bumped past v2.44/v2.45 which github-actions claimed mid-merge for auto-mark of 12 weekend results.) Inline `🔗 Score` link next to kick-off time on upcoming fixtures; `🔗 Match report` on played ones. Opens `google.com/search?q=Home+vs+Away` in a new tab — Google's results page shows live score / lineups / news at the top. `event.stopPropagation()` on the click so it doesn't fight the fixture-expand. Subtle `.live-link` styling (opacity 0.7 → gold on hover) keeps the row uncluttered (`index.html`). |
| 2026-05-22 23:59 BST | Claude (Claude Code, Opus 4.7) | Desktop | `FEATURES.md` backlog gains two items: (1) per-fixture prediction-accuracy badge — `⚽ GOAL` for correct Strong/Likely picks, `🟥 RED CARD` for wrong ones, no badge for Low (coin-flip); (2) fixture hyperlink to Google search so each row opens a Google "Home vs Away" results page with live score/lineups. |
| 2026-05-22 23:50 BST | Claude (Claude Code, Opus 4.7) | Desktop | Added `.nojekyll` marker file at repo root. GitHub Pages was running Jekyll by default and the `jekyll-github-metadata` plugin was failing with "GitHub API credentials … aren't valid", aborting every Pages build since v2.42 (live site was stuck on an older deployed commit). `.nojekyll` tells Pages to skip Jekyll entirely — fine for this pure static-HTML site — so builds now succeed and live catches up. |
| 2026-05-22 23:45 BST | Claude (Claude Code, Opus 4.7) | Desktop | `refresh_today.bat` default window widened from 1 day (today only) to 2 days (today + tomorrow). Friday-evening matchday prep was the common case so making it the default avoids needing `refresh_today.bat 2` every weekend (`refresh_today.bat`). |
| 2026-05-22 23:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | v2.43: re-researched the 10 unplayed Saturday 23 May fixtures via `refresh_today.bat` after the early-exit fix landed. Fresh xG/form, team news and probabilities for all La Liga + Serie A weekend openers (Alavés vs Rayo Vallecano, Real Betis vs Levante, Celta vs Sevilla, Espanyol vs Real Sociedad, Getafe vs Osasuna, Mallorca vs Real Oviedo, Real Madrid vs Athletic, Valencia vs Barça, Milan vs Cagliari, Verona vs Roma) (`index.html`). |
| 2026-05-22 23:00 BST | Claude (Claude Code, Opus 4.7) | Desktop | `refresh_today.py`: removed early-exit when no fixtures needed resetting. Previous logic bailed if everything in the window was already a stub, so a follow-up run after a half-completed previous attempt did nothing. Now always invokes `agent_run()` unless `--no-research`, which picks up both newly-reset stubs and pre-existing ones. |
| 2026-05-22 22:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | v2.42: sidebar Display filters. Past fixtures became a 4-chip mutually-exclusive group (`Off` / `2d` / `Week` / `All`) replacing the old binary toggle. Signal filter added as a 4-chip multi-select group (`All` / `Strong` / `Likely` / `Low`) — clicking `All` clears tiers; clicking any tier removes `All`; deselecting the last tier auto-snaps back to `All` so the view is never empty. Both filters compose via AND. New state vars: `pastWindow`, `signalFilter` (Set); helpers: `pastCutoffTs()`, `passesSignal(f)`; UI: `setPastWindow()`, `toggleSignalFilter()` (`index.html`). |
| 2026-05-20 19:00 BST | Claude (Claude Code, Opus 4.7) | Desktop | Added `Platform` column to the CHANGELOG table (`Desktop` / `Mobile` / `CI`). Backfilled platform values for every existing row by inferring from the "AI Name" tag where possible. Updated the column spec line and added a note in the cross-AI editing section. Makes it easy to see at a glance whether a change came from a desktop IDE session, the mobile app, or github-actions[bot]. |
| 2026-05-20 18:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | `README.md` refresh: removed stale manual-prompt block from "Updating each gameweek"; noted that verify workflow now auto-corrects date/time mismatches (v2.38); documented three agent entry points (`run_agent.bat`, `reset_stubs.bat`, `refresh_today.bat`); noted `UNDERSTAT_ALIASES` for long club names. |
| 2026-05-20 18:00 BST | Claude (Claude Code, Opus 4.7) | Desktop | Fixed missing form dots for **Burnley vs Wolverhampton** and **West Ham vs Leeds United**. Sharper diagnosis than mobile's note: only **Burnley** was actually missing from `UNDERSTAT_TEAM_MAP` — Wolves (slug `Wolverhampton_Wanderers`) and Leeds (slug `Leeds`) resolved correctly already, their fixtures were just researched before the alias fix landed. Added `'Burnley': 'Burnley'` to the map; patched the two fixtures' missing `homeForm`/`awayForm` fields directly with live Understat data (no full re-research — preserves existing analysis) (`agent.py`, `index.html`). |
| 2026-05-20 16:00 BST | Claude (Claude Code) | Mobile | Investigated missing form dots: 16 upcoming fixtures lack homeForm/awayForm. Two root causes — (1) non-Understat clubs (Serie B/playoff/promoted teams e.g. AC Pisa, Cremonese, Como, Levante, Real Oviedo) have no coverage; blank dots are correct for these. (2) Three PL clubs (Burnley, Wolverhampton, Leeds United) should have Understat data but their slugs are missing/wrong in `agent.py` UNDERSTAT_ALIASES — **desktop action needed**: fix aliases and re-run agent on those fixtures. No code changed. |
| 2026-05-20 15:30 BST | Claude (Claude Code) | Mobile | Manually marked missing result: Bournemouth 1-1 Man City (Haaland added-time equaliser; auto-mark missed it as API hadn't finalised score when cron ran). Bumped to `v2.39` (`index.html`, `FEATURES.md`). |
| 2026-05-20 15:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | Collapsible day groups (v2.37): each date header is clickable, chevron rotates, fixture count shown on the right. Hides/reveals all fixtures under that date. Default expanded (`index.html`). |
| 2026-05-20 14:50 BST | Claude (Claude Code, Opus 4.7) | Desktop | Fixed Past Fixtures toggle which appeared to do nothing: `renderFixture` legacy-schema branch referenced a bare `verdict` variable that didn't exist in scope, causing a silent ReferenceError that killed the whole render whenever a past fixture was included. Replaced with `confidence` from `calcConfidence(f)` and collapsed unused tiers (`index.html`). |
| 2026-05-20 13:40 BST | Claude (Claude Code, Opus 4.7) | Desktop | Fixed `Auto-mark results` workflow which had been failing with exit code 2 since the encoding-repair session. Three brittle regex anchors in `scripts/*.js`: (1) `bumpVersion` required `·` separator but topbar drifted to `–`; (2) `loadLeagues` required `// ═══` after the array but those box-drawing chars got mangled to `// â•â•`; (3) `addFeaturesEntry` required `\n\n` (LF) but FEATURES.md uses `\r\n\r\n` (CRLF). All three made tolerant (`scripts/mark-results.js`, `scripts/fetch-fixtures.js`, `scripts/backfill-scores.js`, `scripts/inject-saturday.js`, `scripts/verify-fixtures.js`). |
| 2026-05-20 06:45 BST | Claude (Claude Code) | Mobile | Auto-correct fixture dates/times in verify workflow — script now patches `index.html` directly on date/time mismatch and commits the fix; only "not found" errors still require manual review (`scripts/verify-fixtures.js`, `.github/workflows/verify-fixtures.yml`). |
| 2026-05-20 02:15 BST | Claude (Claude Code, Opus 4.7) | Desktop | Added `refresh_today.py` + `refresh_today.bat` for game-day stat/probability refresh — resets only fixtures kicking off within N days (default 1 = today) back to stubs and triggers the hardened agent pipeline. Reuses the same Understat + SearXNG + LM Studio flow. Use `refresh_today.bat 2` for today + tomorrow. |
| 2026-05-20 01:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | Regenerated all 31 unplayed fixtures using the hardened pipeline on `qwen3.5-9b-claude-4.6-opus-reasoning-distilled-v2`. Wrong-fixture contamination eliminated (Nottingham vs Bournemouth, Brighton vs Man United, etc. now analyse the correct teams). Form dots populated for Brighton, Wolves, Leeds via the alias resolver. Burnley vs Wolves manually set to neutral 33/34/33 — model correctly refused to fabricate when SearXNG returned only Arsenal-vs-Burnley coverage (`index.html`). |
| 2026-05-20 00:45 BST | Claude (Claude Code, Opus 4.7) | Desktop | Added model benchmark harness (`bench.py`, `bench.bat`): caches news once and scores 5 models on objective metrics (JSON validity, sum=100, both-team mention, no fiction, schema completeness, speed). Winner: `qwen3.5-9b-claude-4.6-opus-reasoning-distilled-v2` (perfect 5/5 across all metrics, 24s avg — 40% faster than Gemma 4B which also scored 5/5). Results in `bench_results.md`. |
| 2026-05-19 23:30 BST | Claude (Claude Code, Opus 4.7) | Desktop | Deep-dive fixes to research pipeline (`agent.py`): (1) scored relevance filter — both-team articles prioritised, single-team capped to stop wrong-opponent contamination; (2) `understat_slug()` + alias map fixes silent xG/form failures for long names (Brighton & Hove Albion, Wolverhampton, Leeds United, etc.); (3) hardened prompt — forbids inventing scorelines, wrong-opponent analysis, guessed personnel; (4) post-validation rejects + retries analyses that don't mention both teams. Phrase-based matching for ambiguous clubs (Man Utd/City, Leeds Utd). |
| 2026-05-19 22:44 BST | github-actions[bot] | CI | Auto-marked 1 result (Chelsea 2-1 Tottenham); bumped topbar to `v2.35`. |
| 2026-05-19 21:00 BST | Claude (Claude Code) | Desktop | Form dots feature: last-5 W/D/L dots per team in fixture row; restored `import datetime` and `fetch_xg` calls in `agent.py`; bumped to `v2.34` (`index.html`, `agent.py`). |
| 2026-05-19 19:27 BST | Claude (Claude Code) | Mobile | Mobile responsive overhaul: reduced padding, horizontal scrollable sidebar, two-line team names, uniform verdict badge widths (`index.html`). Reworked verdict system from draw-predictor to general outcome predictor — badge now shows predicted outcome + confidence (Low/Likely/Strong), replaced `drawRates` factor with `momentum`, updated agent prompt to be outcome-neutral (`index.html`, `agent.py`). Merged to `dev`. |
| 2026-05-19 00:19 BST | Claude (Claude Code) | Desktop | Added Understat xG enrichment to agent prompt via `understatapi`; fixed `find_stubs` look-back window (2000→5000); re-researched all 36 unplayed fixtures with xG data; bumped to `v2.32` (`agent.py`, `index.html`). |
| 2026-05-18 20:00 BST | Claude (Claude Code) | Desktop | Renamed app to Football Analyser (`index.html` title + logo, `README.md`); bumped topbar to `v2.31`; renamed Draw Signal column to Signal. |
| 2026-05-18 12:00 BST | Claude (Claude Code) | Desktop | Added backlog item for home/draw/away win probability extension to `FEATURES.md`; bumped topbar to `v2.29` (`index.html`). |
| 2026-05-18 02:00 BST | Claude (Claude Code) | Desktop | Merged 23 auto-marked results into fully-researched fixture data; bumped topbar to `v2.28` (`index.html`, `FEATURES.md`). |
| 2026-05-18 01:00 BST | Claude (Claude Code) | Desktop | Deep-researched all 38 Gameweek 36 stub fixtures across PL, La Liga, Serie A, Bundesliga, Ligue 1 using local LM Studio (Gemma 4B) + SearXNG news pipeline (`index.html`). Bumped topbar to `v2.27`. |
| 2026-05-14 22:30 | Composer (Cursor) | Desktop | Synced full `staging/` tree to repository root (`index.html`, `scripts/`, `.github/workflows/`, `README.md`, `CHANGELOG.md`, `FEATURES.md`, `AGENTS.md`). Topbar bumped to `v2.22` to match prior changelog entry. |
| 2026-05-13 23:36 | Gemini (Antigravity) | Desktop | **Critical:** `scripts/fetch-fixtures.js` — `formatFixtureLiteral` now serializes `teamNews`, `context`, and `bookOdds`, so Monday auto-fetch / prune can no longer wipe researched injury and tactical blocks. **Schedule:** `.github/workflows/mark-results.yml` — 5pm BST pass now runs **Thu–Sun** (`0 16 * * 4,5,6,0`) so midweek results are picked up, not only Sat/Sun. |
| 2026-05-13 23:22 | Gemini (Antigravity) | Desktop | Conducted deep research for three La Liga fixtures (Matchday 35) and injected detailed tactical analysis, form balance, and draw probabilities into the `LEAGUES` data structure (`index.html`). Bumped topbar to `v2.22`. |
| 2026-05-13 23:05 | Gemini (Antigravity) | Desktop | Modified `scripts/fetch-fixtures.js` to strictly use the official API `name` without any custom string cleaning, adhering to "API names are LAW". Manually fetched 51 fixtures to populate staging environment. Bumped to `v2.21`. |
| 2026-05-13 22:45 | Gemini (Antigravity) | Desktop | Removed hardcoded fixtures from index.html to transition to API-only via GitHub Actions (`index.html`). Bumped topbar to `v2.20`. |
| 2026-05-10 20:54 | Codex 5.3 | Desktop | Added aligned fixture-list headings for Fixture, League, Research, Draw Probability, and Draw Signal; tightened table width, improved expanded-panel wrapping, and raised small text sizes for readability (`index.html`). Pending topbar bump to `v2.15`, **FEATURES.md** Done. |
| 2026-05-10 20:26 | Codex 5.3 | Desktop | Updated verdict colours to a traffic-light scale: Low red, Moderate amber, Good green, Strong blue; percentage text and mini-bars now match (`index.html`). Bumped `index.html` to `v2.14`, **FEATURES.md** Done. |
| 2026-05-10 19:05 | Codex 5.3 (Cursor) | Desktop | Changelog datetime column includes **time** (`YYYY-MM-DD HH:MM BST`); updated docs to match (`AGENTS.md`, `README.md`, `FEATURES.md`). Bumped `index.html` to `v2.13`, **FEATURES.md** Done. |
| 2026-05-10 18:45 | Codex 5.3 (Cursor) | Desktop | Reformatted `CHANGELOG.md`: column spec line at top; table **newest-first**; prepend workflow in `AGENTS.md`, `README.md`, `FEATURES.md`. Bumped app to `v2.12` in `index.html`; added **v2.12** in `FEATURES.md`. |
| 2026-05-10 18:39 | Codex 5.3 (Cursor) | Desktop | Replaced `CLAUDE.md` with read-first `AGENTS.md` for multi-AI use and updated `README.md`/`FEATURES.md` to require `CHANGELOG.md` in the edit workflow. |
| 2026-05-10 18:31 | Codex 5.3 (Cursor) | Desktop | Updated `Fair draw odds` label to white for readability and changed `--text3` from `#444` to `#fff` in `index.html`. |

## Cross-AI editing notes

- Prepend **one new row** directly under the table header (above older rows).
- Same-day entries: newest session first; use finer ordering by **time** when needed.
- Run `git status` and `git pull --rebase` before editing.
- **Platform** column: `Desktop` for any IDE/desktop CLI session, `Mobile` for Claude Code Mobile, `CI` for github-actions[bot]. Helps trace where each change originated.
